# Enforce the `engines` floor in package.json: on an older Node, jsdom/undici fail at Vitest
# environment setup with an obscure error and the run reports "0 tests" as if it had passed.
# Better for `npm ci` to refuse up front.
engine-strict=true
